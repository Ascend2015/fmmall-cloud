server:
  port: 8066
spring:
  application:
    name: order-timeout-cancel
eureka:
  client:
    service-url:
      defaultZone: http://xiaoyin:123456@localhost:8761/eureka
feign:
  hystrix:
    enabled: true